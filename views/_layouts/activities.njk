{% extends "views/_layouts/base.njk" %}

{% block content %}

<table class="w-3xl mx-auto mt-5">
  <thead>
    <tr>
      <th>Date</th>
      <th>Distance</th>
      <th>Pace</th>
      <th>Avg HR</th>
      <th>Ascent</th>
    </tr>
  </thead>
  <tbody>
    {% for activity in strava %}
      {% if activity.type == "Run" %}
        <tr class="text-center">
          <td>{{ activity.date }}</td>
          <td>{{ activity.distance }}</td>
          <td>{{ activity.pace }}</td>
          <td>{{ activity.average_heartrate }}</td>
          <td>{{ activity.ascent }}</td>
        </tr>
      {% endif %}
    {% endfor %}
  </tbody>
</table>


{% endblock %}